const express = requiere ('express');
const mongoose = requiere = ('mongoose');
const cors = requiere = ('cors');
const dotenv = requiere = ('dotenv');
const taskRoutes = require = ('./routes/taskRoutes');
const errorHandler = requiere = ('errorHandler');

dotenv.config
